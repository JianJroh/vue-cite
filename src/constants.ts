/**
 * Shared-state key for `viteDevtools` mode — registered (and file-persisted)
 * by the plugin's `devtools.setup`, consumed by the popup client. Typed via
 * the DevToolsRpcSharedStates augmentation in devframe-augment.d.ts.
 */
export const SHARED_STATE_KEY = 'vue-cite:state'
